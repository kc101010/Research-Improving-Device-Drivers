//define a function called main i.e. program entry point
fn main(){
    println!("Hello, World!"); //function prints text to screen, ! indicates that the line is calling from a macro and not a function which are not the same thing
}
